# 观象录本地交付验证报告

验证时间：2026-08-24（Asia/Shanghai）

## 验证环境

- Windows 11 10.0.22631
- Node.js v26.1.0
- npm 11.13.0
- TypeScript 5.9.3（项目开发依赖）

## 已执行命令

```powershell
Set-Location -LiteralPath 'E:\卦'
npm.cmd ci --cache 'E:\卦\.cache\npm' --ignore-scripts --no-audit --no-fund
npm.cmd run validate
```

干净依赖安装成功，项目只安装了一个开发依赖。`validate` 的三段校验全部通过：

- Node 行为与页面契约测试：65/65 通过，0 失败、0 跳过。
- TypeScript 全量类型检查：通过。
- 项目结构与内容检查：通过。

结构检查统计：

- 页面：13
- 桌面版主要入口：7
- 六十四卦：64
- 八卦：8
- 评估题：5
- 单题选项：6、6、6、6、8
- 完整选择组合：10368
- 问题领域：6
- 现实子场景：24
- 观象札记：24
- 本地法律文档：2
- 运行时文件：96
- 运行时代码与数据体积：791596 字节
- 桌面版文件：4
- 桌面版代码与共享数据体积：294490 字节

## 覆盖的关键链路

- 情境观象、今日观象、铜钱点击与明显上滑投掷。
- 五题评估、6—8 个细分选项、10368 种完整组合、八维计分、上下卦、变化爻和 24 类现实子场景。
- 同一领域固定问题下覆盖 2363 种状态摘要和 10246 种完整结果组合，避免大量选择收敛为少数重复文案。
- 64 卦与 384 条爻辞的数据完整性。
- 24 篇观象札记、六类主题、首页每日稳定推荐、分类搜索与展开交互。
- 54 个普通问题和 18 个高风险问题的运行时分流。
- 医疗、法律、投资、彩票、自伤、他伤、人身安全、隐私信息及账号/网络攻击请求拦截。
- 首次同意、法律全文查看、直达路由守卫和安全回跳。
- 本地记录、收藏、草稿、设置、100 条普通记录上限及收藏保留。
- 存储版本迁移、未来版本拒写、瞬时读取失败、结构损坏和静默丢写保护。
- 结果页删除失败后即使存储持续不可读，也会收口状态并给出重试提示。
- 13 页与自定义组件的 WXML 事件绑定、路由、动效关闭、低性能降级和连续点击防护。
- 桌面版首页、领域选择、问题输入、五题评估、结果、自动保存、历史记录与刷新持久化。
- 分享卡不包含用户问题或结构化回答。

## 静态审计

- 运行时代码未发现远程 URL、`wx.request`、云开发、登录、支付、AppSecret 或访问令牌调用。
- 未发现 TODO、FIXME、HACK、占位实现或待补功能标记；静态检查脚本自身包含的检测正则除外。
- 项目所有运行数据、法律文本、规则、页面、样式、测试和文档均位于 `E:\卦`。

## 微信开发者工具验收

- 使用微信开发者工具 Stable 2.02.2608040 和本机测试 AppID 完成官方编译器、模拟器检查。
- 通过 `G:\` 固定英文盘符读取 `E:\卦` 中同一份源码，避开中文工程根路径兼容问题。
- 完全关闭可见开发者工具窗口后，双击 `打开观象录.vbs` 冷启动，模拟器自动进入 `pages/launch/launch` 并显示“观象录”引导界面。
- 已授权仅本机使用的 `guanxiang` 开发者工具客户端，用于固定打开工程与启动页。
- 本轮札记功能变更后，自动验收发现同时存在两个同名开发者工具窗口，未在无法唯一识别目标的情况下盲目点击；因此新增首页卡片与札记页的实时模拟器点击不列为本轮已验证结论。

## Windows 桌面版验收

- 使用系统现有 Microsoft Edge 完成真实浏览器可见性验收，无需下载 Chrome 或安装 Electron。
- 完成工作事业领域选择、中文问题输入、五道评估、结果生成、自动保存、历史查看与页面刷新。
- 刷新后本地记录仍存在，证明浏览器本地持久化链路生效。
- 浏览器控制台最终为 0 错误、0 警告；内容安全策略未再阻止样式或脚本。
- Windows 启动器经过中文路径修复后，可生成标题为“观象录 · 桌面版”的独立 Edge 应用窗口。
- 桌面版和微信小程序分别保存在各自本地存储中，当前不提供跨端同步。

## 尚未完成的外部验收

项目没有用户的正式小程序 AppID，因此本报告不声称已经完成以下项目：

- iOS、Android 真机预览、震动、Canvas 保存及性能验收。
- 体验版、APP 备案、平台隐私配置、服务类目、上传和提审。

当前测试 AppID 仅用于本地开发者工具验收；上传前必须换成真实 AppID，并在提交当天按微信平台最新规则复核类目、内容和隐私配置。
